# config for boolean parameter
parameters:
  "someBooleanParameterName":
    "type": "boolean"
    "referencedParameters": [1, 2]
    "description": "some interesting description"
    "configurations":
      "defaultValue": false
